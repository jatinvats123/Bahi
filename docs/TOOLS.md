# Bahi tools (Swytchcode)

Every external action in Bahi runs through one of these Swytchcode tools. Canonical ids are the real registry ids,
enabled in `.swytchcode/tooling.json` and mirrored in `src/lib/swytch/tools.ts`. Generated by `npm run docs:tools`
from tooling.json and the downloaded wrekenfiles; edit notes in `scripts/gen-tools-doc.ts`.

Inputs go to `swytchcode exec <id> --json` as JSON on stdin: `{ params, body, headers }`. `params` holds both path
and query parameters; header inputs such as `Notion-Version` (default `2025-09-03`) can be overridden in `headers`.

## PayPal (sandbox only)

| Capability | Canonical id | Endpoint | Required inputs | Notes |
| --- | --- | --- | --- | --- |
| Create invoice (draft) (writes) | `invoices.invoicing.invoices.create` | `POST /v2/invoicing/invoices` | body (required: detail, due_amount, gratuity) | Send header `Prefer: return=representation` to get the full invoice back; otherwise PayPal returns only a link. Bahi stores its intent key in `detail.reference`. |
| Send invoice (writes) | `invoices.invoicing.send.create` | `POST /v2/invoicing/invoices/{invoice_id}/send` | `invoice_id` (path); body? (additional_recipients, note, send_to_invoicer, send_to_recipient, subject) | Returns a payer link (or 202 with no body). Resending a sent invoice has no effect. |
| Get invoice | `invoices.invoicing.invoices.get` | `GET /v2/invoicing/invoices/{invoice_id}` | `invoice_id` (path) |  |
| List invoices | `invoices.invoicing.invoices.list` | `GET /v2/invoicing/invoices` | none |  |
| Search invoices | `invoices.invoicing.searchInvoices.create` | `POST /v2/invoicing/search-invoices` | body? (required: creation_date_range, due_date_range, invoice_date_range, payment_date_range, total_amount_range) |  |
| Record a payment on an invoice (simulate a client paying) (writes) | `invoices.invoicing.payments.create` | `POST /v2/invoicing/invoices/{invoice_id}/payments` | `invoice_id` (path); body (required: amount, method) | Only works on SENT invoices; a draft answers 422. Status becomes MARKED_AS_PAID (or PARTIALLY_PAID). |
| Cancel a sent invoice (writes) | `invoices.invoicing.invoices.cancel` | `POST /v2/invoicing/invoices/{invoice_id}/cancel` | `invoice_id` (path); body (additional_recipients, note, send_to_invoicer, send_to_recipient, subject) | Only for sent invoices. Drafts must be deleted instead (Bahi's cancelInvoice does this automatically). |
| Delete a draft invoice (writes) | `invoices.invoicing.invoices.delete` | `DELETE /v2/invoicing/invoices/{invoice_id}` | `invoice_id` (path) |  |
| Refund a captured payment (S5 block-policy target) (writes) | `payments.payment.captures.refund` | `POST /v2/payments/captures/{capture_id}/refund` | `capture_id` (path); body? (amount, custom_id, invoice_id, note_to_payer, payment_instruction) | Same canonical id exists in PayPal.payments_payment_v2 and PayPal.paypal_api; added with `swy add method PayPal@payments_payment_v2.2.0 payments.payment.captures.refund`. |

## Gmail

| Capability | Canonical id | Endpoint | Required inputs | Notes |
| --- | --- | --- | --- | --- |
| List / search messages (unread) | `gmail.user.messages.get` | `GET /gmail/v1/users/{userId}/messages` | `userId` (path, default "me") | Canonical id says `get` but it is the LIST endpoint. Query: `is:unread in:inbox -label:Bahi-Processed`. |
| Get a message | `gmail.user.messages.get1` | `GET /gmail/v1/users/{userId}/messages/{id}` | `userId` (path, default "me"); `id` (path) | `get1` is the single-message endpoint. Kernel max_response_bytes is 100 KB; very large HTML mails may be cut. |
| Send an email (writes) | `gmail.user.send.create1` | `POST /gmail/v1/users/{userId}/messages/send` | `userId` (path, default "me"); body? (classificationLabelValues, historyId, id, internalDate, labelIds, payload, ...) | `create1` is messages/send; `gmail.user.send.create` sends a draft. Body is `{ raw: base64url(RFC 822) }`. |
| Add label / mark read (writes) | `gmail.user.modify.create` | `POST /gmail/v1/users/{userId}/messages/{id}/modify` | `userId` (path, default "me"); `id` (path); body? (addClassificationLabels, addLabelIds, removeClassificationLabelIds, removeLabelIds) | Body `{ addLabelIds, removeLabelIds }`; Bahi removes UNREAD and adds Bahi/Processed. |
| Insert a message into the inbox (seed script) (writes) | `gmail.user.messages.create` | `POST /gmail/v1/users/{userId}/messages` | `userId` (path, default "me"); body? (classificationLabelValues, historyId, id, internalDate, labelIds, payload, ...) | Needs the gmail.insert or full mail scope. `internalDateSource=dateHeader` keeps the seeded dates. |
| Import a message (alternative to insert, with spam scanning) (writes) | `gmail.user.import.create` | `POST /gmail/v1/users/{userId}/messages/import` | `userId` (path, default "me"); body? (classificationLabelValues, historyId, id, internalDate, labelIds, payload, ...) |  |
| List labels (find Bahi/Processed) | `gmail.user.labels.get` | `GET /gmail/v1/users/{userId}/labels` | `userId` (path, default "me") |  |
| Create label Bahi/Processed (writes) | `gmail.user.labels.create` | `POST /gmail/v1/users/{userId}/labels` | `userId` (path, default "me"); body? (color, id, labelListVisibility, messageListVisibility, messagesTotal, messagesUnread, ...) |  |
| Get profile (account check) | `gmail.user.profile.get` | `GET /gmail/v1/users/{userId}/profile` | `userId` (path, default "me") |  |

## Slack

| Capability | Canonical id | Endpoint | Required inputs | Notes |
| --- | --- | --- | --- | --- |
| Post a message (writes) | `slack.chat.postmessage.create` | `POST /chat.postMessage` | `token` (header); body (required: channel) | Slack answers HTTP 200 with `{ ok: false, error }` on failure; Bahi treats that as a failed call. On `not_in_channel` it joins and retries once. |
| List channels (resolve channel ids) | `slack.conversations.list.list` | `GET /conversations.list` | none | Paginated (`cursor`); Bahi resolves names once and caches the ids. |
| Auth test | `slack.auth.test.list` | `GET /auth.test` | `token` (header) |  |
| Join a channel (before first post) (writes) | `slack.conversations.join.create` | `POST /conversations.join` | body? (channel) |  |

## Notion

| Capability | Canonical id | Endpoint | Required inputs | Notes |
| --- | --- | --- | --- | --- |
| Search (find Bahi Ledger by title) | `notion.search.create` | `POST /v1/search` | body? (filter, page_size, query, sort, start_cursor) | Filter `{ property: "object", value: "data_source" }` (API 2025-09-03). |
| Get database (-> data source id) | `notion.databas.get` | `GET /v1/databases/{database_id}` | `database_id` (path) | Canonical id has a typo in the registry: `notion.databas.get`. |
| Get data source schema | `notion.data_source.get` | `GET /v1/data_sources/{data_source_id}` | `data_source_id` (path) |  |
| Update data source schema (exact ledger properties) (writes) | `notion.data_source.update` | `PATCH /v1/data_sources/{data_source_id}` | `data_source_id` (path); body? (description, properties, title) |  |
| Create data source in an existing database (writes) | `notion.data_source.create` | `POST /v1/data_sources` | body? (required: parent, properties) | Needs an existing database parent. The bundle has no POST /v1/databases, so the Bahi Ledger database itself is created once by hand (see npm run setup:notion). |
| Query database (ledger rows) | `notion.query.create` | `POST /v1/data_sources/{data_source_id}/query` | `data_source_id` (path); body? (archived, filter, in_trash, page_size, sorts, start_cursor) | Path param is the DATA SOURCE id, not the database id. |
| Create page (ledger row) (writes) | `notion.page.create` | `POST /v1/pages` | body? (required: parent, properties) | Parent is `{ type: "data_source_id", data_source_id }`. |
| Update page properties (writes) | `notion.page.update` | `PATCH /v1/pages/{page_id}` | `page_id` (path); body? (required: cover, icon) |  |

## Jira Cloud

| Capability | Canonical id | Endpoint | Required inputs | Notes |
| --- | --- | --- | --- | --- |
| Create issue (delivery task) (writes) | `jira.api.issue.create` | `POST /rest/api/3/issue` | body (fields, historyMetadata, properties, transition, update) |  |
| Get issue | `jira.api.issue.get` | `GET /rest/api/3/issue/{issueIdOrKey}` | `issueIdOrKey` (path) |  |
| Search issues (JQL) | `jira.api.jql.create` | `POST /rest/api/3/search/jql` | body (expand, fields, fieldsByKeys, jql, maxResults, nextPageToken, ...) | Uses the current POST /rest/api/3/search/jql. The older `jira.api.search.create1` (/rest/api/3/search) is retired by Atlassian. |
| Get project | `jira.api.project.get2` | `GET /rest/api/3/project/{projectIdOrKey}` | `projectIdOrKey` (path) | `project.get2` is GET /project/{projectIdOrKey}; `project.get` / `get1` are unrelated field-context endpoints. |
| Current user (account check) | `jira.api.myself.list` | `GET /rest/api/3/myself` | none |  |

## Project-level quirks (swytchcode 2.23.5)

- `tooling.json` mode `sandbox` routes every call to each integration's `sandbox_endpoint`, which is `http://localhost` for all five bundles. Bahi runs the project in `production` mode and pins PayPal to `https://api-m.sandbox.paypal.com` for both endpoints (`npm run swytch:configure -- --apply`). The runtime refuses any PayPal call whose endpoint is not the sandbox host.
- Jira's `production_endpoint` ships as the placeholder `https://your-domain.atlassian.net`; `swytch:configure` sets it from `JIRA_BASE_URL`.
- Idempotency is off by default. For PayPal Bahi sets `execution_policy.idempotency = { mode: dynamic, header_name: PayPal-Request-Id }`.
- `swy exec` exit codes differ from the docs: unknown tool = 2 (category `not_found`), network failure = 4, policy block = 6 (category `policy_denied`), approval hold = 7 (plain text, no JSON). Bahi classifies by the JSON `category` first.
- "Required" markers inside nested body schemas (e.g. `due_amount`, `gratuity` on PayPal invoice create) are not enforced by the kernel: a dry run with only detail, invoicer, primary_recipients and items validates. Top-level path params are enforced.
- `swy add` occasionally exits 0 without adding the tool; verify with `swy list tooling` (or `npm run swytch:configure`, which checks every tool).

